# For local development, leave this unset — the Vite dev server proxy
# (configured in vite.config.js) forwards /api to http://localhost:5000.
#
# For production (Vercel/Netlify), set this to your deployed Render backend URL:
# VITE_API_URL=https://capstone-calculator-api.onrender.com/api
VITE_API_URL=
